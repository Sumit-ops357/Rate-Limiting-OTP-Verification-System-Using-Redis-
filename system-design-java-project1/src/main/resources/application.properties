spring.application.name=${SPRING_APPLICATION_NAME:system-design-java-project1}

# Server Configuration
server.port=${SERVER_PORT:8080}
server.servlet.context-path=${SERVER_SERVLET_CONTEXT_PATH:/api}

# Redis Configuration
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}
spring.data.redis.password=${SPRING_DATA_REDIS_PASSWORD:}
spring.data.redis.database=${SPRING_DATA_REDIS_DATABASE:0}
spring.data.redis.timeout=${SPRING_DATA_REDIS_TIMEOUT:2000ms}

# Redis Connection Pool
spring.data.redis.jedis.pool.max-active=${SPRING_DATA_REDIS_JEDIS_POOL_MAX_ACTIVE:8}
spring.data.redis.jedis.pool.max-idle=${SPRING_DATA_REDIS_JEDIS_POOL_MAX_IDLE:8}
spring.data.redis.jedis.pool.min-idle=${SPRING_DATA_REDIS_JEDIS_POOL_MIN_IDLE:0}

# Application Configuration
app.rate-limit.max-requests=${APP_RATE_LIMIT_MAX_REQUESTS:5}
app.rate-limit.window-minutes=${APP_RATE_LIMIT_WINDOW_MINUTES:1}

app.otp.expiry-minutes=${APP_OTP_EXPIRY_MINUTES:5}
app.otp.length=${APP_OTP_LENGTH:6}